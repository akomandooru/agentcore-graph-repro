[project]
name = "agentcore-graph-repro"
version = "0.1.0"
description = "Minimal repro + fix for Strands Graph support in AgentCoreMemorySessionManager"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = [
    "strands-agents>=1.39.0",
    "bedrock-agentcore>=1.9.0",    
]
